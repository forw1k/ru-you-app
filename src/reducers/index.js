const initialState = {
  palette: [
    // { id: "1", color: "#111111" },
    // { id: "2", color: "#555555" },
    // { id: "3", color: "#123456" },
    ['#123123']
  ],
};

export default function (state = initialState, action) {
  return state;
}
